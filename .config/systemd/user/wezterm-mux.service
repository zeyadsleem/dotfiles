[Unit]
Description=WezTerm Multiplexer Terminals Service
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/wezterm-mux-server --daemonize
Restart=on-failure

[Install]
WantedBy=default.target
